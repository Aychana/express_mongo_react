apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fil-rouge-ingress
  namespace: fil-rouge
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
  - host: fil-rouge.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 5001